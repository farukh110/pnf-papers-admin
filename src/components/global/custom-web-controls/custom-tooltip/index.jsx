import { Tooltip } from "primereact/tooltip";
import './index.scss';

const CustomTooltip = (props) => {
    return (
        <>
            <Tooltip {...props} />
        </>
    );
};

export default CustomTooltip;